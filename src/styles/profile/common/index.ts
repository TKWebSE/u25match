/**
 * 共通スタイルのエクスポート
 * 
 * このファイルは以下の責務を持ちます：
 * - プラットフォーム共通のスタイル定義の提供
 * - モバイル・Web両方で使用される基本スタイル
 * - スタイルの重複を避けるための共通化
 * - 一貫したデザインシステムの維持
 * 
 * 含まれるスタイル：
 * - ProfileDetailCommon: プロフィール詳細画面の共通スタイル
 * - ProfileEditCommon: プロフィール編集画面の共通スタイル
 * 
 * 使用例：
 * ```typescript
 * import { ProfileDetailCommon } from '@styles/profile/common';
 * // または
 * import { ProfileDetailCommon } from '@styles/profile';
 * ```
 */
export { ProfileDetailCommon } from './ProfileDetailCommon';
export { ProfileEditCommon } from './ProfileEditCommon';

